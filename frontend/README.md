# swift-agent 前端

Vue 3 + Vite + Element Plus 的对话工作台。

## 启动

```bash
npm install
npm run dev
```

默认监听 `http://localhost:5173`,已配置 Vite 代理 `/api` → `http://localhost:8000`(后端)。

## 构建

```bash
npm run build
```

产物在 `dist/`,可由任意静态服务器托管。
